namespace AvMock.Enums
{
    public enum TemporaryRealTimeScanDisableOptions
    {
        None = 0,

        OneMinute = 1,
        FiveMinutes = 5,
        TenMinutes = 10,
        FifteenMinutes = 15,
        ThirtyMinutes = 30,
        SixtyMinutes = 60,
    }
}